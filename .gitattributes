# fallback on built-in heuristics
# this must be first so later entries will override it
* text=auto

# check out text files with lf, not crlf, on Windows.  
text eol=lf

# These files are text and should be normalized (convert crlf => lf)
*.sh      eol=lf
*.sls     eol=lf

# Windows-specific files get windows endings
*.bat     eol=crlf
*.cmd     eol=crlf
*.ps1     eol=crlf
*.txt     eol=crlf

# Some binary file types for completeness
# (binary is a macro for -text -diff)
*.gif     binary
*.jpg     binary
*.png     binary

